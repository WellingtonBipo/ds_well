name: ds_well
description: "A new Flutter module project."
version: 1.0.0+1
publish_to: none

environment:
  # sdk: ^3.12.0
  sdk: 3.13.0-103.1.beta
  # sdk: 3.12.0-327.4.beta

dependencies:
  flutter:
    sdk: flutter
  utils_well:
    git:
      url: https://github.com/WellingtonBipo/utils_well.git
  skeletonizer: ^2.1.3
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
